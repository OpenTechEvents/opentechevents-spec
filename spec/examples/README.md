# Ejemplos — movidos

> ⛔ **Los ejemplos de esta carpeta se han retirado.** Seguían el boceto anterior y **hoy serían inválidos** contra el schema publicado (`location.venue` como objeto, `kind` en el feed…). Alguien que los copiara se llevaría un documento que el validador rechaza.

👉 **Los ejemplos vigentes están en [`../v0.1/examples/`](../v0.1/examples/)**, y **se validan en CI** contra el schema de la v0.1.

Nada se ha perdido: los campos que aquí se ilustraban y que **aún no están en la v0.1** (`tags`, `cfp`, `offers`, `community`, `image`…) siguen presentes en los ejemplos nuevos, **como extensiones y marcados como tales**. Ver [la política de extensiones](../v0.1/examples/README.md).
